version: '3.8'

services:
  platform:
    build: .
    container_name: musicbot-platform
    restart: unless-stopped
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - /tmp/musicbot:/tmp/musicbot
    environment:
      - PYTHONUNBUFFERED=1
